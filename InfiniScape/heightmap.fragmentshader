#version 330 core

// Ouput data
out vec4 color;

in vec2 UV;
in float height;

// Values that stay constant for the whole mesh.
uniform sampler2D Grass;
uniform sampler2D Snow;

void main(){
	
	color = mix(texture(Grass, UV)*0.5, texture(Snow, UV), max(0.01, (height/10)));
}